import cocktailNameLoad from "./CocktailNameLoad";

const inputRecipeCocktailName = (arg:string): string[] => {
    //TODO: 입력받은 문자열중, 어떤 재료를 입력하려고 했는지 유추하는 기능
    const cocktailNameList = cocktailNameLoad();
    let result: string[] = [];

    return result;
}

export default inputRecipeCocktailName;