import keywordLoad from "./KeywordLoad";

const inputRecipeKeyword = (arg:string): string[] => {
    //TODO: 입력받은 문자열중, 어떤 재료를 입력하려고 했는지 유추하는 기능
    const keywordList = keywordLoad();
    let result: string[] = [];

    return result;
}

export default inputRecipeKeyword;